// Declare the variable and initialize it (optional)
export let TextUserInput: string = "";

// You could also create a setter function if needed
export function setTextUserInput(value: string): void {
  TextUserInput = value;
}
